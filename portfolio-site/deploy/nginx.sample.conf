server {
    listen 80;
    server_name www.yaoliumu.me yaoliumu.me;

    root /www/portfolio-site;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Internal API proxy for the frontend applications.
    # Do not list this address on the public portfolio pages.
    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name admin.yaoliumu.me;

    root /www/pharmacy-admin;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 80;
    server_name client.yaoliumu.me;

    root /www/pharmacy-client;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
